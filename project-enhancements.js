/* Project cards are added after every lesson's theory, labs, and quiz. */
function miniProjects(title){return [{name:'Build the smallest version',steps:[`Define one tiny goal using ${title}.`,'Create a safe local example with one input and one expected output.','Write a short README note explaining what you learned.']},{name:'Predict and experiment',steps:['Predict what will happen before running your example.','Change exactly one input or setting.','Write down the observed difference.']},{name:'Find and fix a bug',steps:['Start with an incorrect assumption or edge case.','Use the common mistakes section to diagnose it.','Make one correction and add a regression check.']},{name:'Improve the first version',steps:['Add validation, error handling, performance awareness, or a security check.','Test one normal case and two edge cases.','Record the before-and-after result.']},{name:'Document and teach it',steps:['Write a concise problem statement.','Add setup, run, and expected-result instructions.','Explain the concept to an imaginary beginner.']},{name:'Real-world mini project',steps:['Choose a small realistic use case.','Break it into data/input, processing, output, and review steps.','Share a final checklist showing it works.']},{name:'Integrate with a nearby concept',steps:['Choose one earlier concept from this level.','Combine both concepts in a small safe workflow.','Explain the responsibility of each concept.']},{name:'Portfolio challenge',steps:['Define success criteria before building.','Create a finished artifact with tests or a verification checklist.','Write lessons learned, limitations, and a next improvement.']}]} 
const projectObserver=new MutationObserver(()=>{const article=out.querySelector('article.panel');if(!article||article.querySelector('.mini-projects'))return;const h=article.querySelector('h1');if(!h)return;const box=document.createElement('section');box.className='lesson-section mini-projects';box.innerHTML=`<h2>10. Mini projects — build it yourself</h2><p>Complete these after the theory, labs, and quiz. Work from Project 1 through Project 8: each one adds a practical skill.</p><div class="grid">${miniProjects(h.textContent).map((p,i)=>`<section class="card"><span class="badge">Project ${i+1} of 8</span><h3>${p.name}</h3><ol>${p.steps.map(s=>`<li>${s}</li>`).join('')}</ol><button class="secondary" data-project="${i}">Mark project started</button></section>`).join('')}</div>`;article.append(box);box.querySelectorAll('[data-project]').forEach(b=>b.onclick=()=>{b.textContent='Project started ✓';b.disabled=true})});projectObserver.observe(out,{childList:true,subtree:true});
